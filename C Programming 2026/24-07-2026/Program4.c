#include <stdio.h>
int main()
{
         int count=200;
         if (count>1000)
         {
            printf("Impose the lockdown");
         }
         else{

            printf(" Release lockdown");
         }
               
    return 0;
}
// output:  Impose the lockdown